# =============================================================================
# CRAFTING CONFIGURATION
# =============================================================================
# Owner: src/crafting.c. Loaded once during normal game boot after object
# templates are available. This file is NOT live-reloadable; restart after a
# change. Unknown keys are ignored. Numeric settings apply only when their
# documented validation passes; malformed values should be treated as operator
# errors and avoided rather than relied upon for fallback behavior.
#
# This controls modern `craft` and modern recipe-based `forge` together. It
# does not control old smith-NPC catalog recipes in forge_items.c, mining,
# material drop rates, object-template stats, or which recipes a player knows.
# Recipe ownership is moving to SQL player_recipes; legacy recipe files are a
# compatibility concern, not a balance setting.
#
# Defaults preserve historical behavior. Comments describe behavior from an
# operator perspective; do not add object vnum ranges or engine limits here.

[settings]
# Maximum recipe item value a player may make is:
#   player level * crafting.level.gate.multiplier
# Both Craft and Forge apply the same gate. Must be a positive integer.
# Historical default: 3.
crafting.level.gate.multiplier=3

# Experience awarded after a successful Craft or Forge is:
#   recipe item value * crafting.experience.per.ival
# Set to 0 to retain items/material behavior while awarding no crafting XP.
# Must be a non-negative integer. Historical default: 1000.
crafting.experience.per.ival=1000

# Multiplies each positive material requirement after the normal recipe plan is
# calculated. Nonzero results round UP so lowering this does not create a free
# required ingredient. Must be greater than zero.
# Historical default: 1.0 (no change).
crafting.material.quantity.multiplier=1.0

# Maximum mortal level used for recipe admission. A target is excluded from
# recipe drops, learning, and recipe-book loading when:
#   item ival > crafting.level.gate.multiplier × this value
# Default 56 is MAXLVLMORTAL. Lower it only when deliberately capping the
# playable level range; values above MAXLVLMORTAL are rejected.
crafting.recipe.max.player.level=56

# When enabled, dynamically created recipe scrolls contain an examine/readable
# material list and Craft/Forge-specific consumables (including magical essence
# when required). `craft ... info`
# and `forge ... info` always show the same live requirements regardless.
# Default: 1. Set 0 only to keep scrolls terse.
crafting.recipe.examine.materials=1

# Recipe-book usability: `craft` and recipe-based `forge` list a short `#`
# and the stable recipe vnum. Commands accept either form (for example,
# `craft info 2` or `forge make 400123`). The list number is per player and
# may change as their recipe book changes; use vnums in scripts or documentation.
#
# Recipe eligibility: A recipe scroll is consumed only after its target passes
# the same permanent validity checks used by modern Craft/Forge. Quest items,
# nonexistent targets, and targets with no salvage-material plan are rejected
# at learning time and remain in the player's inventory. Level is deliberately
# not checked at learning time: players can keep a valid higher-level recipe.
#
# Opt-in/rollback gates for the modern commands. `0` leaves legacy smith-NPC
# forging untouched; it disables only `craft` or the recipe-based `forge`.
crafting.craft.enabled=1
crafting.forge.enabled=1

# Existing support-item vnums. Positive values only are accepted. These are
# content references, not engine storage invariants. Craft and Forge may use
# distinct vnums even when current content maps their essences together.
crafting.craft.essence.vnum=400211
crafting.craft.tool.vnum=400224
crafting.forge.essence.vnum=400211
crafting.forge.tool.vnum=400223

# Lantan Scientific Tools are a Salvage-only consumable. They are checked when
# a salvageable item is torn down, and one set is consumed when Salvage reaches
# its recipe-resolution stage. Invalid configured vnums log a warning and fall
# back to 400227. Restart required after changes.
#
# prevent.breakage: 1 bypasses the normal failed-Salvage-skill destruction roll
# while a set is carried; 0 retains recipe assistance only. Default 1.
# recipe.roll.divisor divides the recipe roll (higher values improve discovery).
# recipe.player.multiplier multiplies the player recipe score (higher improves
# discovery). Both must be integers >= 1. Defaults 15 and 2 preserve the
# historical combined roughly-30x favorable comparison; they do not guarantee
# a recipe and do not permit random/invalid recipe targets.
crafting.salvage.scientific.tools.vnum=400227
crafting.salvage.scientific.tools.prevent.breakage=1
crafting.salvage.scientific.tools.recipe.roll.divisor=15
crafting.salvage.scientific.tools.recipe.player.multiplier=2

# Salvage magical essence tuning. Both values are decimal multipliers >= 0;
# luck.multiplier must be greater than 0. Default 1.0 preserves historical
# Luck thresholds. Effective Luck is floor(character Luck × luck.multiplier).
# chance.multiplier scales the resulting rare Luck-based essence chance after
# all three Luck rolls succeed: 0 disables that rare path; 1.0 preserves it;
# values above 1 increase it and eventually make the final check certain.
# These controls do not affect the guaranteed essence from any item considered
# magical by Craft/Forge, and they do not change enhancement essence drops.
crafting.salvage.essence.luck.multiplier=1.0
crafting.salvage.essence.chance.multiplier=1.0
